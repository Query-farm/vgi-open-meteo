name = "vgi-open-meteo"
main = "src/bin/cf.ts"
compatibility_date = "2025-06-01"

# VGI_SIGNING_KEY is a Wrangler secret (see: wrangler secret put VGI_SIGNING_KEY).
# It seals the per-request state tokens; a stable value is required because
# Workers isolates don't share in-memory state.
